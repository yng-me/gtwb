# add_formats <- function() {
#
#   formats <- .data[["_formats"]]
#
#   if(length(formats) == 0) return(invisible())
#
#   for(i in seq_along(formats)) {
#
#   }
#
# }
#
#
# formats <- tab$`_formats`
#
# for(i in seq_along(formats)) {
#   format <- formats[[i]]
#   cols <- format$cols
#   rows <- format$rows
#
#   print(format$cols)
# }
#
